# Required for production deployments. Use a long, randomly generated secret.
NUXT_APP_AUTH_REQUIRED=true
NUXT_APP_AUTH_USERNAME=admin
NUXT_APP_AUTH_PASSWORD=replace-with-a-long-random-password

# Comma-separated cross-origin browser clients. Same-host requests are allowed
# automatically, so leave this empty unless a separate trusted UI is required.
NUXT_ALLOWED_ORIGINS=

# Optional per-IP limits.
NUXT_API_RATE_LIMIT_PER_MINUTE=600
NUXT_TOKEN_RATE_LIMIT_PER_MINUTE=30

# Keep the diagnostic proxy endpoint disabled outside local development.
NUXT_DEBUG_PROXY_ENABLED=false
NUXT_DEBUG_PROXY_ALLOWED_HOSTS=httpbin.org,api.ipify.org

# Docker Compose maps these values into the app container.
SPFI_AUTH_USERNAME=admin
SPFI_AUTH_PASSWORD=replace-with-a-long-random-password
SPFI_ALLOWED_ORIGINS=
